[project]
name = "hiver"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pandas",
    "pyarrow",
    "scikit-learn",
    "numpy",
    "requests",
    "tqdm",
    "tabulate>=0.10.0",
    "joblib>=1.6.0",
    "scipy>=1.18.1",
]
